import { createSlice } from "@reduxjs/toolkit";


// api calling

// pending
// fullfilled
// error/rejected

const bookSlice = createSlice({
  name: "book",
  initialState: {
    books: [],
    stat
  },
});
