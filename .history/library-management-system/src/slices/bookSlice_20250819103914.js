import { createSlice } from "@reduxjs/toolkit";


// api calling

// pending
// fullfilled
// error/rejected

// loding....

const bookSlice = createSlice({
  name: "book",
  initialState: {
    books: [],
  },
});
